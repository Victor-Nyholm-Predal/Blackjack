#from Pile import Pile
#from Player import Player

#playerPile1 = Pile(0, 0, 0, False)
#playerPile2 = Pile(0, 0, 0, False)
#dealerPile = Pile(0, 0, 0, False)

class Repository:

    def getScore(self, pile):
        #if person == Player:
        #return pile.score
        return 0